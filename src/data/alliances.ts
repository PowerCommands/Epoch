/**
 * Alliance tuning constants.
 *
 * Kept here (not inlined) so the council cadence and related values can be
 * adjusted in one place and shared across the alliance systems.
 */

/** Rounds between automatic Alliance Council meetings for each alliance. */
export const ALLIANCE_COUNCIL_INTERVAL = 25;
